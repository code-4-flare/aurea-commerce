# Claude Code Instructions

Read and follow `AGENTS.md` as the canonical repository instruction file.

Keep this file intentionally small. Put durable project rules in `AGENTS.md`,
feature intent in `docs/product-spec.md`, technical rationale in ADRs, and
bounded implementation tasks in `docs/work-items/`.

## Agent skills

### Issue tracker

Engineering work is tracked locally in `docs/work-items/` through the repository workflow. See `docs/agents/issue-tracker.md`.

### Triage labels

Local work items use the default Matt Pocock triage vocabulary when triage is needed. See `docs/agents/triage-labels.md`.

### Domain docs

This repository uses one shared Aurea commerce context across the storefront and Studio. See `docs/agents/domain.md`.
